import 'package:flutter/material.dart';

class NewReportPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          children: <Widget>[
            Text("new report page", style: TextStyle(fontSize: 30),),
          ],
        ),
      ),
    );
  }
}
